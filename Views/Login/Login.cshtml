@page
@model LoginPageModel

@{
    ViewBag.Title = "Login";
}

<div class="container">
    <article>
        <header>
            <h1>Login</h1>
            <p>The content you requested requires authentication</p>
        </header>
        <div class="col-sm-4">
            <form method="post">
                <div class="form-group">
                    <label>Username</label>
                    <input typeÖ"text" name="Username" class="form-control">
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input type="password" name="Password" class="form-control">
                </div>
                <button class="btn btn-success">Login</button>
            </form>
        </div>
    </article>
</div>